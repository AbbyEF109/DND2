# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'
target ‘DND2’ do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  # Pods for DND2
  pod 'Mixpanel'

  target ‘DND2Tests' do
    inherit! :search_paths
    # Pods for testing
  end
  target ‘DND2UITests' do
    inherit! :search_paths
    # Pods for testing
  end
end